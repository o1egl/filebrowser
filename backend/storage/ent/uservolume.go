// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/filebrowser/filebrowser/backend/storage/ent/user"
	"github.com/filebrowser/filebrowser/backend/storage/ent/uservolume"
	"github.com/filebrowser/filebrowser/backend/storage/ent/volume"
)

// UserVolume is the model entity for the UserVolume schema.
type UserVolume struct {
	config `json:"-"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// VolumeID holds the value of the "volume_id" field.
	VolumeID int `json:"volume_id,omitempty"`
	// Read holds the value of the "read" field.
	Read bool `json:"read,omitempty"`
	// Write holds the value of the "write" field.
	Write bool `json:"write,omitempty"`
	// Delete holds the value of the "delete" field.
	Delete bool `json:"delete,omitempty"`
	// Share holds the value of the "share" field.
	Share bool `json:"share,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserVolumeQuery when eager-loading is set.
	Edges        UserVolumeEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserVolumeEdges holds the relations/edges for other nodes in the graph.
type UserVolumeEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// Volume holds the value of the volume edge.
	Volume *Volume `json:"volume,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserVolumeEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// VolumeOrErr returns the Volume value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserVolumeEdges) VolumeOrErr() (*Volume, error) {
	if e.Volume != nil {
		return e.Volume, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: volume.Label}
	}
	return nil, &NotLoadedError{edge: "volume"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserVolume) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case uservolume.FieldRead, uservolume.FieldWrite, uservolume.FieldDelete, uservolume.FieldShare:
			values[i] = new(sql.NullBool)
		case uservolume.FieldUserID, uservolume.FieldVolumeID:
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserVolume fields.
func (uv *UserVolume) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case uservolume.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				uv.UserID = int(value.Int64)
			}
		case uservolume.FieldVolumeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field volume_id", values[i])
			} else if value.Valid {
				uv.VolumeID = int(value.Int64)
			}
		case uservolume.FieldRead:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field read", values[i])
			} else if value.Valid {
				uv.Read = value.Bool
			}
		case uservolume.FieldWrite:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field write", values[i])
			} else if value.Valid {
				uv.Write = value.Bool
			}
		case uservolume.FieldDelete:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field delete", values[i])
			} else if value.Valid {
				uv.Delete = value.Bool
			}
		case uservolume.FieldShare:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field share", values[i])
			} else if value.Valid {
				uv.Share = value.Bool
			}
		default:
			uv.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserVolume.
// This includes values selected through modifiers, order, etc.
func (uv *UserVolume) Value(name string) (ent.Value, error) {
	return uv.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserVolume entity.
func (uv *UserVolume) QueryUser() *UserQuery {
	return NewUserVolumeClient(uv.config).QueryUser(uv)
}

// QueryVolume queries the "volume" edge of the UserVolume entity.
func (uv *UserVolume) QueryVolume() *VolumeQuery {
	return NewUserVolumeClient(uv.config).QueryVolume(uv)
}

// Update returns a builder for updating this UserVolume.
// Note that you need to call UserVolume.Unwrap() before calling this method if this UserVolume
// was returned from a transaction, and the transaction was committed or rolled back.
func (uv *UserVolume) Update() *UserVolumeUpdateOne {
	return NewUserVolumeClient(uv.config).UpdateOne(uv)
}

// Unwrap unwraps the UserVolume entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (uv *UserVolume) Unwrap() *UserVolume {
	_tx, ok := uv.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserVolume is not a transactional entity")
	}
	uv.config.driver = _tx.drv
	return uv
}

// String implements the fmt.Stringer.
func (uv *UserVolume) String() string {
	var builder strings.Builder
	builder.WriteString("UserVolume(")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", uv.UserID))
	builder.WriteString(", ")
	builder.WriteString("volume_id=")
	builder.WriteString(fmt.Sprintf("%v", uv.VolumeID))
	builder.WriteString(", ")
	builder.WriteString("read=")
	builder.WriteString(fmt.Sprintf("%v", uv.Read))
	builder.WriteString(", ")
	builder.WriteString("write=")
	builder.WriteString(fmt.Sprintf("%v", uv.Write))
	builder.WriteString(", ")
	builder.WriteString("delete=")
	builder.WriteString(fmt.Sprintf("%v", uv.Delete))
	builder.WriteString(", ")
	builder.WriteString("share=")
	builder.WriteString(fmt.Sprintf("%v", uv.Share))
	builder.WriteByte(')')
	return builder.String()
}

// UserVolumes is a parsable slice of UserVolume.
type UserVolumes []*UserVolume
