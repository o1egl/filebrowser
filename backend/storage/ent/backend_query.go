// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/filebrowser/filebrowser/backend/storage/ent/backend"
	"github.com/filebrowser/filebrowser/backend/storage/ent/predicate"
	"github.com/filebrowser/filebrowser/backend/storage/ent/volume"
)

// BackendQuery is the builder for querying Backend entities.
type BackendQuery struct {
	config
	ctx         *QueryContext
	order       []backend.OrderOption
	inters      []Interceptor
	predicates  []predicate.Backend
	withVolumes *VolumeQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BackendQuery builder.
func (bq *BackendQuery) Where(ps ...predicate.Backend) *BackendQuery {
	bq.predicates = append(bq.predicates, ps...)
	return bq
}

// Limit the number of records to be returned by this query.
func (bq *BackendQuery) Limit(limit int) *BackendQuery {
	bq.ctx.Limit = &limit
	return bq
}

// Offset to start from.
func (bq *BackendQuery) Offset(offset int) *BackendQuery {
	bq.ctx.Offset = &offset
	return bq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (bq *BackendQuery) Unique(unique bool) *BackendQuery {
	bq.ctx.Unique = &unique
	return bq
}

// Order specifies how the records should be ordered.
func (bq *BackendQuery) Order(o ...backend.OrderOption) *BackendQuery {
	bq.order = append(bq.order, o...)
	return bq
}

// QueryVolumes chains the current query on the "volumes" edge.
func (bq *BackendQuery) QueryVolumes() *VolumeQuery {
	query := (&VolumeClient{config: bq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := bq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := bq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(backend.Table, backend.FieldID, selector),
			sqlgraph.To(volume.Table, volume.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, backend.VolumesTable, backend.VolumesColumn),
		)
		fromU = sqlgraph.SetNeighbors(bq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Backend entity from the query.
// Returns a *NotFoundError when no Backend was found.
func (bq *BackendQuery) First(ctx context.Context) (*Backend, error) {
	nodes, err := bq.Limit(1).All(setContextOp(ctx, bq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{backend.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (bq *BackendQuery) FirstX(ctx context.Context) *Backend {
	node, err := bq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Backend ID from the query.
// Returns a *NotFoundError when no Backend ID was found.
func (bq *BackendQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bq.Limit(1).IDs(setContextOp(ctx, bq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{backend.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (bq *BackendQuery) FirstIDX(ctx context.Context) int {
	id, err := bq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Backend entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Backend entity is found.
// Returns a *NotFoundError when no Backend entities are found.
func (bq *BackendQuery) Only(ctx context.Context) (*Backend, error) {
	nodes, err := bq.Limit(2).All(setContextOp(ctx, bq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{backend.Label}
	default:
		return nil, &NotSingularError{backend.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (bq *BackendQuery) OnlyX(ctx context.Context) *Backend {
	node, err := bq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Backend ID in the query.
// Returns a *NotSingularError when more than one Backend ID is found.
// Returns a *NotFoundError when no entities are found.
func (bq *BackendQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = bq.Limit(2).IDs(setContextOp(ctx, bq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{backend.Label}
	default:
		err = &NotSingularError{backend.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (bq *BackendQuery) OnlyIDX(ctx context.Context) int {
	id, err := bq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Backends.
func (bq *BackendQuery) All(ctx context.Context) ([]*Backend, error) {
	ctx = setContextOp(ctx, bq.ctx, ent.OpQueryAll)
	if err := bq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Backend, *BackendQuery]()
	return withInterceptors[[]*Backend](ctx, bq, qr, bq.inters)
}

// AllX is like All, but panics if an error occurs.
func (bq *BackendQuery) AllX(ctx context.Context) []*Backend {
	nodes, err := bq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Backend IDs.
func (bq *BackendQuery) IDs(ctx context.Context) (ids []int, err error) {
	if bq.ctx.Unique == nil && bq.path != nil {
		bq.Unique(true)
	}
	ctx = setContextOp(ctx, bq.ctx, ent.OpQueryIDs)
	if err = bq.Select(backend.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (bq *BackendQuery) IDsX(ctx context.Context) []int {
	ids, err := bq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (bq *BackendQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, bq.ctx, ent.OpQueryCount)
	if err := bq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, bq, querierCount[*BackendQuery](), bq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (bq *BackendQuery) CountX(ctx context.Context) int {
	count, err := bq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (bq *BackendQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, bq.ctx, ent.OpQueryExist)
	switch _, err := bq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (bq *BackendQuery) ExistX(ctx context.Context) bool {
	exist, err := bq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BackendQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (bq *BackendQuery) Clone() *BackendQuery {
	if bq == nil {
		return nil
	}
	return &BackendQuery{
		config:      bq.config,
		ctx:         bq.ctx.Clone(),
		order:       append([]backend.OrderOption{}, bq.order...),
		inters:      append([]Interceptor{}, bq.inters...),
		predicates:  append([]predicate.Backend{}, bq.predicates...),
		withVolumes: bq.withVolumes.Clone(),
		// clone intermediate query.
		sql:  bq.sql.Clone(),
		path: bq.path,
	}
}

// WithVolumes tells the query-builder to eager-load the nodes that are connected to
// the "volumes" edge. The optional arguments are used to configure the query builder of the edge.
func (bq *BackendQuery) WithVolumes(opts ...func(*VolumeQuery)) *BackendQuery {
	query := (&VolumeClient{config: bq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	bq.withVolumes = query
	return bq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Backend.Query().
//		GroupBy(backend.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (bq *BackendQuery) GroupBy(field string, fields ...string) *BackendGroupBy {
	bq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BackendGroupBy{build: bq}
	grbuild.flds = &bq.ctx.Fields
	grbuild.label = backend.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.Backend.Query().
//		Select(backend.FieldCreateTime).
//		Scan(ctx, &v)
func (bq *BackendQuery) Select(fields ...string) *BackendSelect {
	bq.ctx.Fields = append(bq.ctx.Fields, fields...)
	sbuild := &BackendSelect{BackendQuery: bq}
	sbuild.label = backend.Label
	sbuild.flds, sbuild.scan = &bq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BackendSelect configured with the given aggregations.
func (bq *BackendQuery) Aggregate(fns ...AggregateFunc) *BackendSelect {
	return bq.Select().Aggregate(fns...)
}

func (bq *BackendQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range bq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, bq); err != nil {
				return err
			}
		}
	}
	for _, f := range bq.ctx.Fields {
		if !backend.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if bq.path != nil {
		prev, err := bq.path(ctx)
		if err != nil {
			return err
		}
		bq.sql = prev
	}
	return nil
}

func (bq *BackendQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Backend, error) {
	var (
		nodes       = []*Backend{}
		_spec       = bq.querySpec()
		loadedTypes = [1]bool{
			bq.withVolumes != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Backend).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Backend{config: bq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, bq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := bq.withVolumes; query != nil {
		if err := bq.loadVolumes(ctx, query, nodes,
			func(n *Backend) { n.Edges.Volumes = []*Volume{} },
			func(n *Backend, e *Volume) { n.Edges.Volumes = append(n.Edges.Volumes, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (bq *BackendQuery) loadVolumes(ctx context.Context, query *VolumeQuery, nodes []*Backend, init func(*Backend), assign func(*Backend, *Volume)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*Backend)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(volume.FieldBackendID)
	}
	query.Where(predicate.Volume(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(backend.VolumesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.BackendID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "backend_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (bq *BackendQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := bq.querySpec()
	_spec.Node.Columns = bq.ctx.Fields
	if len(bq.ctx.Fields) > 0 {
		_spec.Unique = bq.ctx.Unique != nil && *bq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, bq.driver, _spec)
}

func (bq *BackendQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(backend.Table, backend.Columns, sqlgraph.NewFieldSpec(backend.FieldID, field.TypeInt))
	_spec.From = bq.sql
	if unique := bq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if bq.path != nil {
		_spec.Unique = true
	}
	if fields := bq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, backend.FieldID)
		for i := range fields {
			if fields[i] != backend.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := bq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := bq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := bq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := bq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (bq *BackendQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(bq.driver.Dialect())
	t1 := builder.Table(backend.Table)
	columns := bq.ctx.Fields
	if len(columns) == 0 {
		columns = backend.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if bq.sql != nil {
		selector = bq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if bq.ctx.Unique != nil && *bq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range bq.predicates {
		p(selector)
	}
	for _, p := range bq.order {
		p(selector)
	}
	if offset := bq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := bq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BackendGroupBy is the group-by builder for Backend entities.
type BackendGroupBy struct {
	selector
	build *BackendQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (bgb *BackendGroupBy) Aggregate(fns ...AggregateFunc) *BackendGroupBy {
	bgb.fns = append(bgb.fns, fns...)
	return bgb
}

// Scan applies the selector query and scans the result into the given value.
func (bgb *BackendGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bgb.build.ctx, ent.OpQueryGroupBy)
	if err := bgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BackendQuery, *BackendGroupBy](ctx, bgb.build, bgb, bgb.build.inters, v)
}

func (bgb *BackendGroupBy) sqlScan(ctx context.Context, root *BackendQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(bgb.fns))
	for _, fn := range bgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*bgb.flds)+len(bgb.fns))
		for _, f := range *bgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*bgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BackendSelect is the builder for selecting fields of Backend entities.
type BackendSelect struct {
	*BackendQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bs *BackendSelect) Aggregate(fns ...AggregateFunc) *BackendSelect {
	bs.fns = append(bs.fns, fns...)
	return bs
}

// Scan applies the selector query and scans the result into the given value.
func (bs *BackendSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bs.ctx, ent.OpQuerySelect)
	if err := bs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BackendQuery, *BackendSelect](ctx, bs.BackendQuery, bs, bs.inters, v)
}

func (bs *BackendSelect) sqlScan(ctx context.Context, root *BackendQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bs.fns))
	for _, fn := range bs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
